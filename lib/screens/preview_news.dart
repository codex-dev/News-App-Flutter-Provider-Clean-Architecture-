import 'package:flutter/material.dart';

class PreviewNews extends StatelessWidget {
  const PreviewNews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}